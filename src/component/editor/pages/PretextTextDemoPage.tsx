import Editor from "../Editor";

/**
 * Optional demo: Pretext `prepare` / `layout` text block (see `src/component/textbox.tsx`).
 * Add `{ path: "/editor/text-demo", element: <PretextTextDemoPage /> }` in `routes.tsx` to enable.
 */
export default function PretextTextDemoPage() {
  return <Editor />;
}
