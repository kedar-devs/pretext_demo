import EditorForm from "../components/form";

/**
 * Route-level page for the article editor (Pretext body, images, stores).
 * Register this component in `src/router/routes.tsx` — e.g. path `/editor`.
 */
export default function ArticleEditorPage() {
  return <EditorForm />;
}
